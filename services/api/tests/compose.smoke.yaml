# Isolated local smoke stack. No production environment or public listener.
services:
  database:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: mural_container_test
      POSTGRES_USER: mural_test
      POSTGRES_PASSWORD: isolated-smoke-test-only
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mural_test -d mural_container_test"]
      interval: 2s
      timeout: 2s
      retries: 20
    volumes:
      - smoke_data:/var/lib/postgresql/data
  migrate:
    image: mural-server-smoke:local
    build: ..
    environment:
      DATABASE_URL: postgresql://mural_test:isolated-smoke-test-only@database:5432/mural_container_test
    command: ["node", "dist/src/migrate.js"]
    depends_on:
      database:
        condition: service_healthy
  api:
    image: mural-server-smoke:local
    build: ..
    environment:
      DATABASE_URL: postgresql://mural_test:isolated-smoke-test-only@database:5432/mural_container_test
      PUBLIC_ORIGIN: http://localhost:8080
    ports: ["127.0.0.1::8080"]
    read_only: true
    tmpfs: [/tmp]
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    depends_on:
      migrate:
        condition: service_completed_successfully
  tests:
    image: mural-server-smoke:local
    environment:
      TEST_DATABASE_URL: postgresql://mural_test:isolated-smoke-test-only@database:5432/mural_container_test
    command: ["sh", "-c", "node --test --test-concurrency=1 dist/tests/*.test.js"]
    depends_on:
      database:
        condition: service_healthy
volumes:
  smoke_data:
